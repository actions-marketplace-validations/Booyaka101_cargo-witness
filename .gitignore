node_modules/
*.log
.DS_Store
.tmp-e2e/
# dist/ is intentionally NOT ignored — GitHub Actions must ship the bundled
# dist/action.js (built with `npm run build:action`).
.tmp-demo/
tmp-measure/

# Local build notes, not for the public repo
PROGRESS.md
